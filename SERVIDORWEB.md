el servidor web sirve para todo lo que es frontend y que interptreta el navegador, pero el backend no lo traduce por tanto usa el servidor de aplicaciones para que le traduzca el backend y se lo devuelva:

servidor web usa --> {nginx o Apache(modulos SSl LDAP. se cae si tiene mucho trafico pero lo solucionaremos con kubernetes)}.

